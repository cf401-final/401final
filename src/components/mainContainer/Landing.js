import React from 'react';
import { Typography } from '@mui/material';

const Landing = () => {
  return (
    <div id="landing">
      <h1>Welcome to Jangle.</h1>
      <Typography>Please sign or register to continue.</Typography>
    </div>
  );
};

export default Landing;
